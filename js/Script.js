function showBook(elem) {
    elem.parentNode.removeChild(elem);
    document.getElementById('bookOpen').style.display
    document.getElementById('wrapper').innerHTML += `"qåwdqåpdlqåwp asuhis" `;
}
